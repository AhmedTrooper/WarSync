# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Analytics
# Vercel Analytics (already integrated) does NOT require an env var.
# Keep this only if you decide to also use Google Analytics later.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
